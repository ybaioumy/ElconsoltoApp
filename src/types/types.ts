export type ArticleComponentProps = {
  imageSource: string;
  heading: string;
  description: string;
  vertical?: boolean;
};

export type ArticleItemProps = {
  id: number;
  heading: string;
  description: string;
  imageSource: string;
};